Note the zvbi distribution is included so the proper source can be
matched to the included shared libraries.  The .so libs work on the
CentOS version that we deployed in 2007, and may have to be rebuild for
other targets.
